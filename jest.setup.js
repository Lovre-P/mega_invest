// jest.setup.js
// Optional: import '@testing-library/jest-dom/extend-expect'; // For frontend tests

// You can add global setup mocks or configurations here if needed.
// For backend tests like auth.ts, this might be minimal or empty initially.

// Example: Mock environment variables if your tests depend on them
// process.env.ADMIN_EMAIL = 'test@example.com';
// process.env.ADMIN_PASSWORD = 'testpassword';
// process.env.JWT_SECRET = 'testsecret';
